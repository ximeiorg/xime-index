id: number_rows
name: 数字行
author: kingzcheung
description: 在标准全键盘上方新增一行数字，中文键盘长按出符号、英文键盘直接上屏。
tags:
  - 布局
repo: https://github.com/cz-archive/number_rows
license: GPL-3.0
appVersion: ">=3.0.0"
requiresSchemes: []
# 截图：仅详情页展示（1~5 张）
screenshots:
  - https://cdn.jsdelivr.net/gh/cz-archive/number_rows@master/images/number_rows.jpg
currentVersion: "1.0.0"
versions:
  - version: "1.0.0"
    date: "2026-09-24"
    changelog: 首个版本：标准 4 行布局 + 顶部数字行（中英各 5 行）。
    # 纯文本布局（无 themes/fonts 资源）→ 单文件形态，直接引用 xime.custom.yaml。
    # URL 必须不可变：这里固定到 commit（不打 tag / 不用分支名）。
    downloadUrl:
      - url: https://cdn.jsdelivr.net/gh/cz-archive/number_rows@master/xime.custom.yaml
        sha256: "88d6e994f193a74ae8be95e134f46cbccbb39d970aed68e6e720dff560b0e271"
        size: "2.5 KB"